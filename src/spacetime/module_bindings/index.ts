// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 2.10.1 (commit 3d7607082ab47257adeb7c25164536511237f2b5).

/* eslint-disable */
/* tslint:disable */
import {
  DbConnectionBuilder as __DbConnectionBuilder,
  DbConnectionImpl as __DbConnectionImpl,
  SubscriptionBuilderImpl as __SubscriptionBuilderImpl,
  TypeBuilder as __TypeBuilder,
  Uuid as __Uuid,
  convertToAccessorMap as __convertToAccessorMap,
  makeQueryBuilder as __makeQueryBuilder,
  procedureSchema as __procedureSchema,
  procedures as __procedures,
  reducerSchema as __reducerSchema,
  reducers as __reducers,
  schema as __schema,
  t as __t,
  table as __table,
  type AlgebraicTypeType as __AlgebraicTypeType,
  type DbConnectionConfig as __DbConnectionConfig,
  type ErrorContextInterface as __ErrorContextInterface,
  type Event as __Event,
  type EventContextInterface as __EventContextInterface,
  type Infer as __Infer,
  type QueryBuilder as __QueryBuilder,
  type ReducerEventContextInterface as __ReducerEventContextInterface,
  type RemoteModule as __RemoteModule,
  type SubscriptionEventContextInterface as __SubscriptionEventContextInterface,
  type SubscriptionHandleImpl as __SubscriptionHandleImpl,
} from "spacetimedb";

// Import all reducer arg schemas
import ChooseInterventionReducer from "./choose_intervention_reducer";
import EnsureSessionReducer from "./ensure_session_reducer";
import ResetMissionReducer from "./reset_mission_reducer";
import RunSimulationReducer from "./run_simulation_reducer";
import SeedCityMemoryReducer from "./seed_city_memory_reducer";
import SeedDataSourceReducer from "./seed_data_source_reducer";
import SeedInterventionReducer from "./seed_intervention_reducer";
import SeedMissionReducer from "./seed_mission_reducer";
import SetModeReducer from "./set_mode_reducer";

// Import all procedure arg schemas

// Import all table schema definitions
import CityMemoryRow from "./city_memory_table";
import DataSourceRow from "./data_source_table";
import InterventionRow from "./intervention_table";
import MissionRow from "./mission_table";
import MissionStateRow from "./mission_state_table";
import PlayerSessionRow from "./player_session_table";
import SimulationRunRow from "./simulation_run_table";

/** Type-only namespace exports for generated type groups. */

/** The schema information for all tables in this module. This is defined the same was as the tables would have been defined in the server. */
const tablesSchema = __schema({
  cityMemory: __table({
    name: 'city_memory',
    indexes: [
      { accessor: 'id', name: 'city_memory_id_idx_btree', algorithm: 'btree', columns: [
        'id',
      ] },
    ],
    constraints: [
      { name: 'city_memory_id_key', constraint: 'unique', columns: ['id'] },
    ],
  }, CityMemoryRow),
  dataSource: __table({
    name: 'data_source',
    indexes: [
      { accessor: 'id', name: 'data_source_id_idx_btree', algorithm: 'btree', columns: [
        'id',
      ] },
    ],
    constraints: [
      { name: 'data_source_id_key', constraint: 'unique', columns: ['id'] },
    ],
  }, DataSourceRow),
  intervention: __table({
    name: 'intervention',
    indexes: [
      { accessor: 'id', name: 'intervention_id_idx_btree', algorithm: 'btree', columns: [
        'id',
      ] },
      { accessor: 'missionId', name: 'intervention_mission_id_idx_btree', algorithm: 'btree', columns: [
        'missionId',
      ] },
    ],
    constraints: [
      { name: 'intervention_id_key', constraint: 'unique', columns: ['id'] },
    ],
  }, InterventionRow),
  mission: __table({
    name: 'mission',
    indexes: [
      { accessor: 'id', name: 'mission_id_idx_btree', algorithm: 'btree', columns: [
        'id',
      ] },
    ],
    constraints: [
      { name: 'mission_id_key', constraint: 'unique', columns: ['id'] },
    ],
  }, MissionRow),
  missionState: __table({
    name: 'mission_state',
    indexes: [
      { accessor: 'missionId', name: 'mission_state_mission_id_idx_btree', algorithm: 'btree', columns: [
        'missionId',
      ] },
    ],
    constraints: [
      { name: 'mission_state_mission_id_key', constraint: 'unique', columns: ['missionId'] },
    ],
  }, MissionStateRow),
  playerSession: __table({
    name: 'player_session',
    indexes: [
      { accessor: 'identity', name: 'player_session_identity_idx_btree', algorithm: 'btree', columns: [
        'identity',
      ] },
    ],
    constraints: [
      { name: 'player_session_identity_key', constraint: 'unique', columns: ['identity'] },
    ],
  }, PlayerSessionRow),
  simulationRun: __table({
    name: 'simulation_run',
    indexes: [
      { accessor: 'id', name: 'simulation_run_id_idx_btree', algorithm: 'btree', columns: [
        'id',
      ] },
      { accessor: 'missionId', name: 'simulation_run_mission_id_idx_btree', algorithm: 'btree', columns: [
        'missionId',
      ] },
    ],
    constraints: [
      { name: 'simulation_run_id_key', constraint: 'unique', columns: ['id'] },
    ],
  }, SimulationRunRow),
});

/** The schema information for all reducers in this module. This is defined the same way as the reducers would have been defined in the server, except the body of the reducer is omitted in code generation. */
const reducersSchema = __reducers(
  __reducerSchema("choose_intervention", ChooseInterventionReducer),
  __reducerSchema("ensure_session", EnsureSessionReducer),
  __reducerSchema("reset_mission", ResetMissionReducer),
  __reducerSchema("run_simulation", RunSimulationReducer),
  __reducerSchema("seed_city_memory", SeedCityMemoryReducer),
  __reducerSchema("seed_data_source", SeedDataSourceReducer),
  __reducerSchema("seed_intervention", SeedInterventionReducer),
  __reducerSchema("seed_mission", SeedMissionReducer),
  __reducerSchema("set_mode", SetModeReducer),
);

/** The schema information for all procedures in this module. This is defined the same way as the procedures would have been defined in the server. */
const proceduresSchema = __procedures(
);

/** The remote SpacetimeDB module schema, both runtime and type information. */
const REMOTE_MODULE = {
  versionInfo: {
    cliVersion: "2.10.1" as const,
  },
  tables: tablesSchema.schemaType.tables,
  reducers: reducersSchema.reducersType.reducers,
  ...proceduresSchema,
} satisfies __RemoteModule<
  typeof tablesSchema.schemaType,
  typeof reducersSchema.reducersType,
  typeof proceduresSchema
>;

/** The tables available in this remote SpacetimeDB module. Each table reference doubles as a query builder. */
export const tables: __QueryBuilder<typeof tablesSchema.schemaType> = __makeQueryBuilder(tablesSchema.schemaType);

/** The reducers available in this remote SpacetimeDB module. */
export const reducers = __convertToAccessorMap(reducersSchema.reducersType.reducers);

/** The procedures available in this remote SpacetimeDB module. */
export const procedures = __convertToAccessorMap(proceduresSchema.procedures);

/** The context type returned in callbacks for all possible events. */
export type EventContext = __EventContextInterface<typeof REMOTE_MODULE>;
/** The context type returned in callbacks for reducer events. */
export type ReducerEventContext = __ReducerEventContextInterface<typeof REMOTE_MODULE>;
/** The context type returned in callbacks for subscription events. */
export type SubscriptionEventContext = __SubscriptionEventContextInterface<typeof REMOTE_MODULE>;
/** The context type returned in callbacks for error events. */
export type ErrorContext = __ErrorContextInterface<typeof REMOTE_MODULE>;
/** The subscription handle type to manage active subscriptions created from a {@link SubscriptionBuilder}. */
export type SubscriptionHandle = __SubscriptionHandleImpl<typeof REMOTE_MODULE>;

/** Builder class to configure a new subscription to the remote SpacetimeDB instance. */
export class SubscriptionBuilder extends __SubscriptionBuilderImpl<typeof REMOTE_MODULE> {}

/** Builder class to configure a new database connection to the remote SpacetimeDB instance. */
export class DbConnectionBuilder extends __DbConnectionBuilder<DbConnection> {}

/** The typed database connection to manage connections to the remote SpacetimeDB instance. This class has type information specific to the generated module. */
export class DbConnection extends __DbConnectionImpl<typeof REMOTE_MODULE> {
  /** Creates a new {@link DbConnectionBuilder} to configure and connect to the remote SpacetimeDB instance. */
  static builder = (): DbConnectionBuilder => {
    return new DbConnectionBuilder(REMOTE_MODULE, (config: __DbConnectionConfig<typeof REMOTE_MODULE>) => new DbConnection(config));
  };

  /** Creates a new {@link SubscriptionBuilder} to configure a subscription to the remote SpacetimeDB instance. */
  override subscriptionBuilder = (): SubscriptionBuilder => {
    return new SubscriptionBuilder(this);
  };
}

